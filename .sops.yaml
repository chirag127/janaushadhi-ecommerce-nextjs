# sops config — same age key as the workspace root (one keypair for everything).
# Encrypt this repo's .env.local:  sops -e .env.local > .env.local.enc
# (SOPS_AGE_KEY_FILE=~/.config/sops/age/keys.txt). Plaintext stays gitignored.
creation_rules:
  - path_regex: \.env(\.local)?$
    age: age1c40qjamejzrp9cajle9g0dss25mmsmyaq6uaa2pgmyr3pflsy4qspgw5c4
